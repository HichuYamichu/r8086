bits 16
mov si, bx
mov dh, al
mov cl, byte 12
mov ch, byte 244
mov cx, word 12
mov cx, word 65524
mov dx, word 3948
mov dx, word 61588
mov al, byte [bx + si]
mov bx, word [bp + di]
mov dx, word [bp +0]
mov ah, byte [bx + si +4]
mov al, byte [bx + si +4999]
mov word [bx + di], cx
mov byte [bp + si], cl
mov byte [bp +0], ch
mov ax, word [bx + di -37]
mov word [si -300], cx
mov dx, word [bx -32]
mov byte [bp + di], byte 7
mov word [di +901], word 347
mov bp, word [5]
mov bx, word [3458]
mov ax, word [2555]
mov ax, word [16]
mov word [2554], ax
mov word [15], ax
add bx, word [bx + si]
add bx, word [bp +0]
add si, byte 2
add bp, byte 2
add cx, byte 8
add bx, word [bp +0]
add cx, word [bx +2]
add bh, byte [bp + si +4]
add di, word [bp + di +6]
add word [bx + si], bx
add word [bp +0], bx
add word [bp +0], bx
add word [bx +2], cx
add byte [bp + si +4], bh
add word [bp + di +6], di
add byte [bx], byte 34
add word [bp + si +1000], byte 29
add ax, word [bp +0]
add al, byte [bx + si]
add ax, bx
add al, ah
add ax, word 1000
add al, byte 226
add al, byte 9
sub bx, word [bx + si]
sub bx, word [bp +0]
sub si, byte 2
sub bp, byte 2
sub cx, byte 8
sub bx, word [bp +0]
sub cx, word [bx +2]
sub bh, byte [bp + si +4]
sub di, word [bp + di +6]
sub word [bx + si], bx
sub word [bp +0], bx
sub word [bp +0], bx
sub word [bx +2], cx
sub byte [bp + si +4], bh
sub word [bp + di +6], di
sub byte [bx], byte 34
sub word [bx + di], byte 29
sub ax, word [bp +0]
sub al, byte [bx + si]
sub ax, bx
sub al, ah
sub ax, word 1000
sub al, byte 226
sub al, byte 9
cmp bx, word [bx + si]
cmp bx, word [bp +0]
cmp si, byte 2
cmp bp, byte 2
cmp cx, byte 8
cmp bx, word [bp +0]
cmp cx, word [bx +2]
cmp bh, byte [bp + si +4]
cmp di, word [bp + di +6]
cmp word [bx + si], bx
cmp word [bp +0], bx
cmp word [bp +0], bx
cmp word [bx +2], cx
cmp byte [bp + si +4], bh
cmp word [bp + di +6], di
cmp byte [bx], byte 34
cmp word [4834], byte 29
cmp ax, word [bp +0]
cmp al, byte [bx + si]
cmp ax, bx
cmp al, ah
cmp ax, word 1000
cmp al, byte 226
cmp al, byte 9
